export interface ProgressBarProps {
    children: React.ReactNode;
}
export interface PackageDistribution {
    id: number
    name: string
    address: string
    postcode: string
    city: string
    country: string
    lat: string
    lng: string
  }